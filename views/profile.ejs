<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f4f4f4; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
    .profile-card { background: white; padding: 40px; border-radius: 10px; box-shadow: 0 0 20px rgba(0,0,0,0.1); width: 420px; text-align: center; }
    h1 { color: #28a745; }
    .info { margin: 15px 0; padding: 10px; background: #f8f9fa; border-radius: 5px; }
    .label { font-weight: bold; color: #555; }
    .success { color: green; }
    .warning { color: #d9534f; }
    .logout-btn { display: inline-block; margin-top: 20px; padding: 12px 24px; background: #dc3545; color: white; text-decoration: none; border-radius: 5px; }
    .logout-btn:hover { background: #c82333; }
  </style>
</head>
<body>
  <div class="profile-card">
    <h1>Welcome, <%= username %>!</h1>
    <p class="success">You are successfully logged in</p>

    <div class="info">
      <span class="label">User ID:</span> <%= userId %><br>
      <span class="label">Username:</span> <%= username %>
    </div>

    <div class="info">
      <span class="label">Session expires:</span><br>
      <strong><%= expiresIn %></strong>
    </div>

    <% if (isLongLived) { %>
      <p class="success">Remember Me enabled – You’ll stay logged in for 30 days</p>
    <% } else { %>
      <p class="warning">Session ends when you close the browser</p>
    <% } %>

    <a href="/logout" class="logout-btn">Logout</a>
  </div>
</body>
</html>